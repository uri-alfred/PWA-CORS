# Para hacer la prueba una vez descargado el código

En la ruta /api-react en linea de comandos
Ejecutar el comando
### `npm install`
Luego
### `npm start`

En la ruta /servidor en linea de comandos
Ejecutar el comando
### `npm install`
Luego
### `node index.js`
